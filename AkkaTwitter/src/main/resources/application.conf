akkatwitter {

  # you can set these values using -Dakkatwitter.twitter.accessToken="<actualAccessToken>" and so on
  twitter {
    accesToken = "****"
    accessTokenSecret = "****"
    consumerKey = "****"
    consumerSecret = "****"
  }

  cassandra {
    keyspace = "zuehlke"
    hosts = [ "localhost" ]
    port = 9042
  }

}